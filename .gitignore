# Local dev data
pb_data/
pb_hooks/

# Event store
events.db
events.db-*

# Binaries
*.exe
pocketcqrs

# Editor
.vscode/
.idea/

# Browser-probe dependencies (see pocketcqrs-dashboard/probe/README.md).
# The probes are checked in; the browser driver they need is not.
node_modules/
package-lock.json

# probe leftovers: the editor probe writes into pb_functions/, and a PUT keeps
# a .prev copy as its undo. Neither belongs in the repo.
pb_functions/probe_*.js
*.js.prev
